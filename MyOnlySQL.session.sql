CREATE DATABASE salesDB;
The create database command is for creating database in my SQL environment
The salesDB is the name of the database

CREATE DATABASE IF NOT EXIst salesDB;
The create database if not exist is for creating a database in case it does not exist
if it exist it will not create a new one

USE DATABASE salesDB;
The use of this command is for using the command you have just created

DROP DATABASE demo;
This command is for permanently removing the database and all its content including tables, data and views

